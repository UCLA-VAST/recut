steps:
  - label: Build
    command:
      - nix-build
      - nix-shell --run "echo OK"
      - nix-instantiate --eval -E '(import <nixpkgs> {}).lib.version'
      - pwd
      - ls bin
      - ls data
    
    artifact_paths:
      - test_detail.json
